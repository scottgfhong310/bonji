# 依賴
node_modules/

# 系統檔
.DS_Store
Thumbs.db

# 編輯器 / 工具
.vscode/
.claude/

# 日誌
*.log

# 匯出的 JSON（保留資料夾結構，不追蹤內容）
/public/download/bonji/*
!/public/download/bonji/.gitkeep

# ── 字型：刻意不收，以及理由 ──────────────────────────────────────
# 本 repo 是 **public**。字型是二進位著作物，收進來就是對全世界再散布。
# 判定依據見 DESIGN.md §11 與
# 「My Projects/Siddham/SIDDHAM_DOMAIN_GOVERNANCE.md」§9.2〔owner 2026-08-17 拍板〕。
#
# ⚠️ 兩個容易讀反的地方，各記一次：
#   1. `OS/2 fsType`（本三支為 0x0000 / 0x0008 / 0x0000）回答的是
#      「能不能**嵌進文件／PDF**」，**不是散布許可**。
#   2. **沒有授權條款 ≠ 可以散布。** 著作權預設保留，要有**明示授予**才有散布權。
#      對照就是證據：可散布的字型會把授權寫在自己身上（Noto 的 name13／name14），
#      這三支沒有。**一個著作權人姓名不是一份授權。**
#
# public/apps/bonji/fonts/Mojikm13.TTF（2.7MB，family `Mojikyo M119`）
#   — 字型內 name0 ＝ `Copyright(c)1998-2010 AI-NET.Corporation.All rights reserved.`、
#     商標 `Mojikyo`。**明示保留權利**，風險等級最高。
#     文字鏡研究会 2019-02-12 解散、官網關閉，**無官方發佈管道**（故 app 內不附連結，
#     只說明現況——指向第三方鏡像等於重做我們正在移除的那件事）。
#
# public/apps/bonji/fonts/Siddham.ttf（4.8MB，family `Siddam`，CBETA）
#   — 字型內 Copyright 欄只有 `CBETA` 四個字，**無條款、無授權 URL**；
#     CBETA 下載頁對該檔亦無任何授權條款（站台的 CC BY-NC-SA 3.0 TW 是**網站**的授權，
#     且為 **NC**，未言明涵蓋字型二進位）。
#   — 改讀本機安裝版（`@font-face { src: local('Siddam') }`），**那正是 CBETA 自己的
#     散布方式**：它的下載頁就是叫人裝進系統字型。
#
# public/apps/bonji/fonts/UniSiddham.ttf（3.5MB，family `UniSiddham`）
#   — `Siddham.ttf` 的前身，早在 7a41442 就從工作樹換掉了，但**一直留在 git 歷史裡**，
#     而且是前兩支清完之後最大的物件（2026-08-17 一併自歷史清除）。
#   — 授權比另兩支更弱：**連 Copyright 欄都沒有**，無商標、無 name13／14，
#     全部身家只有 `Version 6.02;;UniSiddham;2016;FL714`
#     （`FL714` 與 `Siddam` 同一個產生器簽章，推測同屬 CBETA 一系——**推測，未經證實**）。
#   — ⚠️ **這一行是補的，不是一開始就有的。** 前兩支是照著工作樹裡看得到的檔案列的，
#     而它不在工作樹裡、只在歷史裡，於是第一輪整個漏掉——**「資料夾裡有什麼」
#     不等於「這個 repo 散布過什麼」**。日後再有字型進來，要問的是後者。
#   — 現行程式**沒有任何地方引用這個 family**（`uniSiddham` 那一組自 7a41442 起
#     改用 `Siddam` 算圖），故它純粹是不該再出現的舊物，不是待接回的功能。
#
# ⚠️ NotoSansSiddham-Regular.woff2 **不在此列、照常收錄**：它 name13 寫明 SIL OFL 1.1、
#    name14 指向 https://scripts.sil.org/OFL，並附 fonts/OFL.txt。可散布的長這樣。
/public/apps/bonji/fonts/Mojikm13.TTF
/public/apps/bonji/fonts/Siddham.ttf
/public/apps/bonji/fonts/UniSiddham.ttf
